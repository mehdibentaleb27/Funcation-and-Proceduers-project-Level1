

#include <iostream>
using namespace std;
int main()
{
    short width, length,area_Rectangle;
    cout << "Please Enter a Number for the width\n";
    cin >> width;
    cout << "Please Enter a Number for the length\n";
    cin >> length;
    area_Rectangle = width * length;

    cout << "\nArea Rectangle :" << area_Rectangle;
}

